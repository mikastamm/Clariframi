import { DropdownListOptions } from './dropdown-list-options';

describe('DropdownListOptions', () => {
  it('should create an instance', () => {
    expect(new DropdownListOptions()).toBeTruthy();
  });
});
